# Upstash Redis (rate limiting) — create a free database at https://console.upstash.com/
# and copy its REST API credentials from the database details page.
# Must be set as real environment variables in Vercel (via `vercel env add`) before deploying Task 12.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
